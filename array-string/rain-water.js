/**
 * @param {number[]} height
 * @return {number}
 */
var trap = function(height) {
    var totalWater = 0;

    for(let i=0; i<height.length; i++){

      var start = -1;
      if(start < 0 && height[i] > 0){
        start = i; //store the index
      }

      if(start > 0 && )


    }
};
